fun compare(a: Number, b: Number): String {
    let comp =
        if (< a b) {
            "a is less than b"
        } else
        if (> a b) {
            "a is greater than b"
        } else {
            "a and b are equal"
        };
    return compa
};