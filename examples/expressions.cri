/* expressions are written in Polish notation */
let exp = (+ 12 (* 7 2)); /* expressions must be assigned */

let a = (- 5 10); /* this is 5 - 10 = -5 */
let b = (* 2 2);

let is_a_greater_than_b = if (> a b) {
    true
} else {
    false
};